export type Clientes = {
  id: number;
  nombre: string;
  direccion: string;
  telefono: string;
  nacionalidad: string;
  imageUrl: string;
};

export type Companias = {
    id: number;
    nombre: string;
    direccion: string;
    telefono: string;
    comentarios: string;
}
